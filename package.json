{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run build:worker",
    "build:worker": "tsc -p tsconfig.worker.json && node scripts/install-pages-worker.mjs",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "dompurify": "^3.3.1",
    "tailwindcss": "^4.1.18",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/three": "^0.182.0",
    "typescript": "^5.9.3",
    "vite": "^6.0.0"
  }
}
